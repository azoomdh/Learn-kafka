> phải dùng linux vì chạy app docker trên windows tốn ram
> 
> dùng debian lxqt để tốn ít ram và vẫn đủ chức năng

<br/>

cài docker engine

> https://docs.docker.com/engine/install/debian

<br/>

chạy docker engine không cần quyền root

> https://docs.docker.com/engine/install/linux-postinstall/

<br/>

cài docker desktop

> https://docs.docker.com/desktop/setup/install/linux/debian/

<br/>

cài docker bị lỗi không kết nối được

> (GPT tạo keyword) : 
> 
> **System-wide DNS configuration using systemd-resolved**

<br/>

> (GPT tạo prompt) :
> 
> Hướng dẫn cấu hình DNS toàn cục (global DNS) trên Debian bằng systemd-resolved thay vì cấu hình từng kết nối NetworkManager.
